import { createContext } from 'react'
import { ControlsContext } from '@components/VideoPlayer/components/Controls/types'

// @ts-ignore Stupid context
export const Context = createContext<ControlsContext>()
